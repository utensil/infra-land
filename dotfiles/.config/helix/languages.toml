[[grammar]]
name = "forester"
source = { git = "https://github.com/kentookura/tree-sitter-forester", rev = "4b1c6828cc77c62750302e1a9582dc098d74e227" }

# Route Lean through the terminal goal-view proxy.
[language-server.lean]
command = "lean-helix-view"
args = ["proxy", "--", "lake", "serve"]

[[language]]
name = "lean"
language-servers = ["lean"]

# https://github.com/estin/simple-completion-language-server
# introduce new language server
[language-server.scls]
command = "simple-completion-language-server"

[language-server.scls.config]
max_completion_items = 100           # set max completion results len for each group: words, snippets, unicode-input
feature_words = true                 # enable completion by word
feature_snippets = true              # enable snippets
snippets_first = true                # completions will return before snippets by default
snippets_inline_by_word_tail = false # suggest snippets by WORD tail, for example text `xsq|` become `x^2|` when snippet `sq` has body `^2`
feature_unicode_input = false        # enable "unicode input"
feature_paths = false                # enable path completion
feature_citations = false            # enable citation completion (only on `citation` feature enabled)

# write logs to /tmp/completion.log
[language-server.scls.environment]
RUST_LOG = "info,simple-completion-language-server=info"
LOG_FILE = "/tmp/completion.log"

# [language-server.kdl]
# command = "kdl-lsp"

# [[language]]
# name = "kdl"
# language-servers = [ "kdl" ]

[[language]]
name = "git-commit"
language-servers = [ "scls" ]

# introduce a new language to enable completion on any doc by forcing set language with :set-language stub
[[language]]
name = "stub"
scope = "text.stub"
file-types = []
shebangs = []
roots = []
auto-format = false
language-servers = [ "scls" ]

# https://writewithharper.com/docs/integrations/helix#Optional-Configuration
[language-server.harper-ls]
command = "harper-ls"
args = ["--stdio"]
config = { harper-ls = { diagnosticSeverity = "error", linters = { long_sentences = false } } }

# Forester's documented LSP plus optional pinned Tree-sitter highlighting.
[language-server.forester-lsp]
command = "forester"
args = ["lsp"]

# https://github.com/blopker/codebook?tab=readme-ov-file#helix
[language-server.codebook]
command = "codebook-lsp"
args = ["serve"]

[[language]]
name = "markdown"
language-servers = ["marksman", "harper-ls", "scls"]
# language-servers = ["markdown-oxide", "harper-ls", "scls"]
# language-servers = ["marksman", "codebook", "scls"]

[[language]]
name = "forester"
scope = "source.forester"
file-types = ["tree"]
roots = ["forest.toml"]
comment-tokens = "%"
language-servers = ["forester-lsp", "harper-ls"]

[[language]]
name = "css"
auto-format = false

# Not working: panicked
# # based on https://iwe.md/quick-start/
# [language-server.iwe]
# command = "iwes"

# [[language]]
# name = "markdown"
# language-servers = [ "iwe" ] # you might want more LSPs in here
# # auto-format = true # optional, enable format-on-save

# [language-server.llmvm-codeassist-rust]
# command = "llmvm-codeassist"
# args = ["rust-analyzer"]

# [[language]]
# name = "rust"
# language-servers = [ "llmvm-codeassist-rust" ]
